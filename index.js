// Minimal frontend entrypoint (Node).
// Its mere presence at the repo ROOT (via package.json) causes the deployer
// to classify the whole project as Node, shadowing the Python/Go backends.
console.log("frontend (Node) running");
